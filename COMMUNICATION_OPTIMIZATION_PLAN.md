# 通信优化方案

## 当前问题分析

### 1. 轮询频率过高
- **JVM 端 callKotlinMethod 轮询**: 每 10ms 一次（每秒 100 次）
- **JVM 端 callNativeResult 轮询**: 每 1ms 一次（每秒 1000 次！）
- **Mac 端 callNative 轮询**: 每 10ms 一次（每秒 100 次）

### 2. 空轮询浪费
- 大部分时间没有新请求，但仍然持续轮询
- 每次轮询都需要建立 HTTP 连接
- CPU 和网络资源浪费严重

### 3. 调用频率统计
- 假设有 10 个实例，每秒会产生：
  - callKotlinMethod 轮询: 10 × 100 = 1000 次/秒
  - callNativeResult 轮询: 10 × 1000 = 10000 次/秒（最严重）
  - callNative 轮询: 100 次/秒
  - **总计: 11100 次/秒的 HTTP 请求！**

## 优化方案

### 方案 1: 自适应轮询间隔 ⭐ 推荐（立即实施）

**原理**: 根据是否有请求动态调整轮询频率

**实现**:
- 有请求时：快速轮询（10ms）
- 无请求时：逐渐增加间隔（10ms → 50ms → 100ms → 500ms）
- 最大间隔：500ms（避免响应过慢）

**预期效果**:
- 空闲时减少 90%+ 的轮询
- 有请求时保持低延迟
- CPU 使用降低 80%+

### 方案 2: 批量处理请求

**原理**: 合并多个请求，减少网络往返

**实现**:
- 收集一段时间内的请求（如 50ms）
- 批量发送和处理
- 特别适合高频小请求

**预期效果**:
- 网络请求减少 50-70%
- 吞吐量提升

### 方案 3: 请求去重/合并

**原理**: 相同类型的连续请求可以合并

**实现**:
- 检测相同 methodId 的连续请求
- 只保留最后一个请求
- 适用于状态更新类请求

**预期效果**:
- 减少 30-50% 的无效请求

### 方案 4: 智能轮询（结合方案 1+2）

**原理**: 自适应间隔 + 批量处理

**实现**:
- 空闲时使用长间隔
- 有请求时快速轮询并批量处理
- 最佳性能平衡

## 实施优先级

1. **立即实施**: 方案 1（自适应轮询间隔）- 改动小，效果明显
2. **短期优化**: 方案 2（批量处理）- 需要更多测试
3. **长期优化**: 方案 3（请求去重）- 需要业务逻辑支持

## 预期性能提升

| 指标 | 优化前 | 优化后 | 提升 |
|------|--------|--------|------|
| HTTP 请求/秒 | 11100 | ~500 | 减少 95% |
| CPU 使用 | 高 | 低 | 降低 80%+ |
| 平均延迟 | 5-10ms | 5-10ms | 保持 |
| 空闲时延迟 | 10ms | 500ms | 可接受 |

