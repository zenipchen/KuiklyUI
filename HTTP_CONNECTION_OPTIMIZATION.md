# HTTP 连接优化方案

## 问题分析

### "Can't assign requested address" 错误原因

1. **连接未复用**：每次请求都创建新连接，立即 `disconnect()`
2. **TIME_WAIT 状态**：连接关闭后进入 TIME_WAIT（约 60 秒），占用端口
3. **高频请求**：`callNative` 和轮询请求频繁，短时间内创建大量连接
4. **端口耗尽**：系统可用端口数有限（通常 32768-65535），高并发时容易耗尽

### 当前问题

- 使用 `HttpURLConnection`，不支持连接池
- 每次请求都 `disconnect()`，无法复用连接
- 没有 HTTP/1.1 Keep-Alive
- 轮询频率高（100ms），产生大量连接

## 解决方案

### 方案 1: 使用 HttpClient 连接池（推荐）⭐

**优势**：
- 自动连接复用和 Keep-Alive
- 内置连接池管理
- 减少端口占用
- 提高性能

**实现**：
- 使用 Java 11+ `HttpClient` 替代 `HttpURLConnection`
- 通过 `HttpClientManager` 管理连接池
- 按 instanceId 和请求类型分离连接池

### 方案 2: 批量处理请求

**优势**：
- 减少请求次数
- 降低连接创建频率

**实现**：
- 收集一段时间内的 `callNative` 请求
- 批量发送和处理

### 方案 3: 优化轮询机制

**优势**：
- 减少空轮询
- 降低连接数

**实现**：
- 使用长轮询（Long Polling）
- 增加轮询间隔（已实现自适应间隔）

### 方案 4: 请求去重/合并

**优势**：
- 减少重复请求
- 降低网络开销

**实现**：
- 检测相同 methodId 的连续请求
- 只保留最后一个请求

## 实施优先级

1. **P0（立即）**：切换到 HttpClient 连接池
2. **P1（短期）**：批量处理 callNative 请求
3. **P2（长期）**：实现长轮询

